# GATE G3 — INTERFACE BASELINE
**Attempt:** 1 · **Date:** 2026-08-27 · **Rigour:** 3 · **Owner:** `[interaction-designer]`
**Artifact:** `docs/INTERFACE.md` · **Result: PASS**

Criteria 1–8 apply at Rigour 3. Criteria 9–10 are Rigour 4/5, not in force.

| # | Criterion | Evidence | Verdict |
| --- | --- | --- | --- |
| 1 | Interface type chosen and justified | §1 — CLI + read-only API, no GUI. One operator, batch work, scriptable. BRIEF §4 excludes dashboards | **PASS** |
| 2 | Every user-facing `REQ` → a `UI-###` | Orphan check found **5 uncovered** (`REQ-064`–`067`, `REQ-069`); `UI-009`, `UI-010` added. Now **0** | **PASS after remediation** |
| 3 | Five UI principles assessed | §3 — structure, simplicity, visibility, feedback, tolerance, each against a named command | **PASS** |
| 4 | Every error state has a message and recovery path | §5 — 9 codes, each with cause and next action | **PASS** |
| 5 | Undo/confirm for every destructive action | §4 — 4 actions enumerated with guard and reversibility | **PASS** |
| 6 | Primary flows documented end to end | §6 — flows A (ingest), B (diagnose), C (recover) | **PASS** |
| 7 | Accessibility considered | §8 — colour never sole meaning, `NO_COLOR`, non-TTY, `--json`, screen-reader-safe output | **PASS** |
| 8 | Feedback latency budget stated | §7 — <1 s / 1–10 s / >10 s tiers; `Ctrl-C` safe with cursor resume | **PASS** |

## Notes

**The CLI was not treated as exempt.** Ìlànà names that as the standard failure at this gate, and
the reason is real: a CLI demands recall where a GUI offers recognition, so it needs the principles
*more*. `appmd doctor` and `appmd source list` exist specifically to convert recall into
recognition — the operator can see the state of their setup and each source's rate limits rather
than remembering them.

**Two error codes exist because measurement showed the failure would otherwise be silent:**
`SAMPLING_NOT_STRATIFIED` (R3: shards are size-ordered; an offset-0 run reports "mean skill size
10 bytes" and looks successful) and `NO_DLQ_CONFIGURED` (`DEC-025`: Cloudflare deletes exhausted
messages permanently when no DLQ is set). Both turn silent data problems into loud startup failures.

**Overrides:** none. **Design elements:** 81 (71 `DES` + 10 `UI`). **Decision:** advance to phase 04 Construction.
