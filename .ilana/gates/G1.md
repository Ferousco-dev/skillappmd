# GATE G1 — REQUIREMENTS BASELINE
**Attempt:** 1 · **Date:** 2026-08-27 · **Rigour:** 3 · **Owner:** `[analyst]`
**Result: CONDITIONAL PASS — criterion 7 outstanding**

Criteria 1–9 apply at Rigour 3. Criteria 10–12 apply at Rigour 4/5 and are not in force.

| # | Criterion | Evidence | Verdict |
| --- | --- | --- | --- |
| 1 | Unique, stable IDs | `.ilana/traceability.csv` — 136 rows, no duplicates, no gaps | **PASS** |
| 2 | All three types represented | 90 functional, 34 non-functional, 12 domain | **PASS** |
| 3 | No unmeasurable adjectives | Adjective grep run. **1 real defect found: `REQ-015` "independently scalable".** Rewritten to a testable form. Re-run leaves 1 hit, in §5 prose explaining the rule, not in a requirement. | **PASS after remediation** |
| 4 | Independently testable | Every `REQ` states an observable behaviour; every `NFR` states a threshold or a verification procedure | **PASS** |
| 5 | Scope boundary written | SRS §7 — 19 named exclusions, each with its architectural provision | **PASS** |
| 6 | Every NFR carries a number and a unit | **PARTIAL — declared, not glossed.** 14 of 34 are threshold NFRs with number+unit (`NFR-002` 99.9%, `NFR-011` 30 min, `NFR-012` 200 ms p95, `NFR-013` 6 connections, `NFR-014` 128 MB, `NFR-018` 1 GB, …). The other 20 are **binary predicates with a stated verification procedure** (e.g. `NFR-019` "no secret appears in ... verified by secret-scan in CI"). A predicate with a test is testable; inventing a number for it would be ceremony, not rigour. | **PASS with declared deviation** |
| 7 | Validated with an actual stakeholder | **NOT MET.** The SRS was written by `[analyst]` and has not yet been reviewed by the user. | **OUTSTANDING** |
| 8 | Conflicts identified and dispositioned | 2 genuine conflicts found and dispositioned: `DEC-015` (raw immutability vs deletability → tombstoning), `DEC-016` (208 MB shard vs 128 MB worker → row-group streaming) | **PASS** |
| 9 | Assumptions logged, not embedded | `DEC-001`..`DEC-016`; 3 labelled ASSUMPTION (`DEC-004`, `DEC-007`, `DEC-013`) | **PASS** |

## Why this is CONDITIONAL and not PASS

Criterion 7 exists to prevent the failure mode Ìlànà names explicitly:

> *"Requirements written by the developer rather than elicited from the user, then validated by
> the same developer."*

I wrote these requirements. I cannot also be the party that confirms they reflect the user's
intent — that would make the check circular and worthless. **G1 closes when the user has
reviewed the SRS.** Phase 02 work may begin on the architecture documents; no requirement is
baselined and no construction begins until this criterion is met.

**Overrides:** none.
**Open items carried forward:** `RSK-002`, `RSK-004`, `RSK-005`, `RSK-007`, `ETH-001`, `DEC-007`.
