{
  "name": "@appmd/connector-gitskills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitSkillsCorpusConnector. REQ-003. Batch runtime only (DATABASE.md \u00a77).",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "parquet-wasm": "0.7.2"
  },
  "appmdNotes": "parquet-wasm is quarantined to this package (CR-005). It sits behind the CorpusReader seam and must never be imported by skill-core, ports, ingestion or apps."
}
