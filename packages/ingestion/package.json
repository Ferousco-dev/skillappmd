{
  "name": "@appmd/ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pipeline stages. Imports ports only - never an adapter, never a vendor SDK (NFR-028).",
  "main": "src/index.js",
  "scripts": { "test": "node --test test/*.test.js" }
}
